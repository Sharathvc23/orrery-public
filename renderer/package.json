{
  "name": "orrery-a2ui-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Reference renderer for Orrery A2UI surfaces over AG-UI SSE. Framework-free, zero dependencies, static-servable.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "node --test tests/e2e/*.e2e.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}